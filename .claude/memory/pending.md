# Pending Items

- [ ] User: `sudo apt install jq` — universal hooks need jq; until then the security gate and session-context hooks exit non-blocking.
- [x] WP1 skeleton (M0) — done 2026-09-14.
- [ ] Group A: WP2 scanner, WP3 mounts, WP4 geometry/data, WP5 widgets (agents) + WP6 tree model/controller (main).
- [ ] WP8 Explorer must ship: drag-reorderable + resizable columns, header-click sorting (asc/desc indicator, model-owned), right-click header menu for column visibility (Owner, Type), all persisted in settings `explorer.*` (user request 2026-09-14).
