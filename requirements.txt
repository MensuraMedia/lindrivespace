# LinDriveSpace runtime dependencies.
# On Debian/Ubuntu/Mint prefer the system packages (they carry the GTK bindings):
#   sudo apt install python3-gi gir1.2-gtk-3.0 python3-cairo python3-psutil python3-pyudev fonts-ubuntu policykit-1
# Only use pip in a venv created with --system-site-packages.
PyGObject>=3.42.0
pycairo>=1.20.0
psutil>=5.9
pyudev>=0.24
