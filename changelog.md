# Project Change Log

> Local record of all changes. Does NOT depend on git. Updated every time a change is made.

| Date-Time | Change Description |
|-----------|-------------------|
| 2026-09-14T18:40:00 | Project created. Concept & technical design written to docs/CONCEPT-AND-TECHNICAL-DESIGN.md. |
| 2026-09-14T18:55:00 | Design mockups (Directions A–D + component sheet) written to docs/mockups/lindrivespace-mockups.html and published. |
| 2026-09-14T19:05:00 | Build plan approved (green-light). Direction: B Overview first screen + D insight panel; allocated primary; decimal GB; 150 px sidebar; light theme v1.1; .deb only. |
| 2026-09-14T19:10:00 | WP0: universal-instruction-set v2026.04 deployed (.claude/ agents, skills, roles, hooks, commands, rules, settings), git initialised with origin, dev venv (ruff, mypy, pytest). |
| 2026-09-14T20:30:00 | README.md written (purpose, features, screens, design language, architecture, install, roadmap); mockup boards rendered to docs/mockups/images/*.png; mockup page gained single-board capture mode (#dirA…#sheet). |
| 2026-09-14T21:10:00 | WP1 (M0): Gtk.Application skeleton — header bar, 150 px sidebar with logo + icon nav, Gtk.Stack with 4 stub pages, token-driven CSS (app.css + @define-color), frozen core contracts (fsnode, events, options), settings store, test harness (core headless, ui on DISPLAY), --smoke/--screenshot. Design doc §16 errata added. |
| 2026-09-14T22:40:00 | WP2: core scanner (iterative scandir DFS, pre-order events, mount boundary, hard-link dedupe, cancel/pause, ScanThread), core/units.py formatters, stdlib-only scanner_cli.py (NDJSON, pkexec-ready), 37 tests, bench ≈150k entries/s. |
| 2026-09-14T22:40:00 | WP3: core/mounts.py (psutil + mountinfo + lsblk merge on (maj:min, mountpoint), statvfs usage, group_by_disk, lazy pyudev MountMonitor fd), 27 tests with captured fixtures. Design doc §16 gained the bind-mount merge note. |
| 2026-09-14T22:40:00 | WP5: ui/widgets — PercentBarRenderer (Cairo cell renderer, fixed height), RingGauge, MountCard (+MountCardData, scan-requested/selected signals), KpiTile; 11 offscreen-rendered tests. |
| 2026-09-14T22:40:00 | WP6: models/tree_model.py (3-column lazy TreeStore, draw-time formatting via cell data funcs, LIS move-based model-owned sort, running totals, O(1) prepend inserts) and services/scan_controller.py (16 ms drain with 8 ms budget, GObject signals, FakeProducer); 7 tests incl. real end-to-end scan. |
| 2026-09-14T23:20:00 | WP4: core/treemap.py (squarify, layout_node, hit_test), core/classify.py (FileClass, summarize_top_files), core/snapshot.py (gzip JSON with iterative writer/reader, list, diff); 27 tests. config/settings.py typed for mypy --strict. |
| 2026-09-14T23:50:00 | WP11: services/privilege.py (PrivilegedScan producer over pkexec, SIGINT/SIGSTOP control, exit-code mapping), core/event_codec.py (NDJSON round trip), data/bin/lindrivespace-scan-helper (arg-validated execv launcher), polkit policy; 35 tests. |
| 2026-09-15T00:20:00 | WP8: Explorer page — ExplorerTree (reorderable/resizable/hideable columns, header-click sort with indicator, visibility menu, persisted order/widths/sort), ScanToolbar, Breadcrumb, TreeContextMenu, header progress pill, status bar; 7 tests. Orchestrator: session-scoped window fixture (tests/ui/conftest.py), checked-button + progressbar CSS, root auto-expand on first batch. |
| 2026-09-15T00:30:00 | Decision (user): drop Direction C; Explorer = A table + D insight panel, panel must collapse (toolbar toggle + F9, persisted). Overview retained as the mounts feature. |
| 2026-09-15T00:45:00 | Feature request (user): Favorites — star folders/files, "Favorites" sidebar page listing them, click opens the Explorer space view of that item. Registry entry, settings key and page stub added; WP14 launched. |
| 2026-09-15T01:10:00 | WP10: services/export.py (CSV/JSON/top-files, iterative), services/actions.py (file manager, terminal, clipboard, trash, reveal via FileManager1), Snapshots page (list, open, delete, compare with coloured diff); 14 tests. |
| 2026-09-15T01:10:00 | WP7: services/mounts_service.py (threaded refresh, udev fd watch with 300 ms debounce, 30 s usage refresh), models/mounts_model.py, Overview page (KPI strip, per-disk FlowBox of flat mount cards, show-hidden, refresh); 8 tests. Fixed pseudo-fs leak and card wrapping. |
| 2026-09-15T01:40:00 | Explorer: percent column header renamed "% of Parent" → "Share %" (tooltip explains parent share); column hiding confirmed via header right-click menu (all but Name) with Reset; mockup updated. |
| 2026-09-15T02:00:00 | WP14: services/favorites.py (FavoritesStore over settings, changed signal, scan_target for files) and Favorites page (card rows, rename, open in file manager, remove, click → Explorer scan of that item, pending_reveal for files); 19 tests. |
| 2026-09-15T02:20:00 | WP12: Settings page (Appearance, Scanning defaults, Exclusions, Mounts, Explorer, About; immediate persistence; accessible names + mnemonics), PrefRow/PrefGroup helpers, WCAG contrast test for both themes; light fg_dim darkened to #8c9098 to pass 3:1. 22 tests. |
| 2026-09-15T03:30:00 | WP9: collapsible insight panel (Treemap / Top files / Types / Age) docked in the Explorer; toolbar toggle + F9 + collapse button, persisted, auto-hide < 1100 px. Orchestrator fixed a GtkPaned collapse and on-demand treemap layout. 10 tests. |
| 2026-09-15T03:30:00 | Explorer drill-down to files (user): expanding a folder lists its files live (single scandir, lstat sizes) interleaved with sub-folders, each with its own Share % bar; capped at the 2,000 largest plus a "… N more files" summary row; folders with only files now get an expander; double-click on a file reveals it in the file manager; Type column classifies files. |
| 2026-09-15T03:30:00 | Settings › Mounts: Primary / Secondary mountpoint selectors (user); Overview badges those cards (PRIMARY accent / SECONDARY warm grey) and lists them first. |
| 2026-09-15T03:30:00 | Favorites wired into the Explorer (user): toolbar star, Ctrl+D, context-menu Add/Remove, Favorites page click scans that item and reveals a file favourite in its folder. Toolbar "Columns" button + context-menu "Columns…" open the column visibility menu. |
| 2026-09-15T03:30:00 | Error trapping & logging (user): lindrivespace/logsetup.py — rotating log in ~/.cache/lindrivespace/logs (1 MB × 5), uncaught exceptions on the main thread, worker threads and GTK callbacks logged and shown in an in-window error bar with Details/Open log; GTK/GLib warnings and criticals captured; --debug flag; Settings › About shows the log path. 6 tests. |
| 2026-09-15T03:30:00 | Ring gauges replaced by bar gauges on mount cards (user), same accent/warn/danger thresholds (ui/widgets/bar_gauge.py). |
| 2026-09-15T06:10:00 | Fix: progress signal used 32-bit gint for byte totals → "could not convert type int to gint" once a scan passed 2 GB (from the new error bar/log). Now int64. |
| 2026-09-15T06:10:00 | Startup auto-scan (user): services/scan_registry.py keeps one model per root and runs a queue (primary, secondary, /, then every physical mount) 1.5 s after launch; Overview subtitle and cards show a spinner + "Scanning… N entries"; Explorer follows the active scan until the user picks one; finished scans are reused (Scan/Rescan forces). Settings › Scanning "Scan all mounts on startup". |
| 2026-09-15T06:10:00 | Explorer toolbar spacing/margins fixed (6 px gaps, 8 px margins, linked Allocated/Apparent pair, text "Columns" button); header pill gained a spinner. |
| 2026-09-15T07:30:00 | Scan display moved from the header pop-up to a full-width strip at the top of the content area (user): spinner, path, real progress bar (scanned ÷ mount used bytes, or last scan total for folders), scanned-of-total, percent and rate, estimated countdown, Pause/Stop, queue chip. No "entries" wording. Mockups of three variants: docs/mockups/scan-strip-mockups.html (A built). |
| 2026-09-15T07:30:00 | Hardware and Glossary pages registered in the sidebar (WP16, WP15 in progress). |
| 2026-09-15T08:10:00 | Scan strip switched to variant B (user): big percent, bar + key/value row (scanning · scanned · of used · rate · elapsed), square queue chips (done = bold green, no checkmark; running = accent), large countdown with Pause/Stop. |
| 2026-09-15T08:40:00 | WP16: Hardware page — core/hardware.py (system, DMI board/firmware, CPU, memory, lspci storage controllers, lsblk + /sys disk details, /proc/diskstats I/O rates, filesystems, drive types; all unprivileged, guarded) and the page (8 cards, disks + live I/O tables, Refresh/Copy report); 36 tests. |
| 2026-09-15T09:00:00 | WP15: Glossary page — core/glossary.py with 120 terms in 7 categories (disks & partitions, filesystems incl. ext/ext2/3/4, mounting, space & sizes, directories & the tree, storage subsystems, best practices), search + category chips, expandable rows with See-also links; 23 tests. |
| 2026-09-15T09:30:00 | docs/HANDOFF.md written (state, how to run/verify, architecture, decisions, open items); scripts/backup.sh added; local backup taken. |
| 2026-09-15T10:20:00 | Sidebar: Settings button gets the same top/bottom borders as the other buttons plus 8 px bottom margin. Hardware: visible grid lines on the Disks and I/O tables (separator colour → highlight); every card has a Copy button (PrefGroup.enable_copy/as_text). Overview: uniform 300 px cards in every group, star on each card adds the mountpoint to Favorites, all hidden/virtual mounts in one compact "hidden & virtual" group. Mockups of four Overview view types: docs/mockups/overview-views-mockups.html. |
| 2026-09-15T10:50:00 | Mount cards fixed at 300 × 132 px with single-line ellipsized labels; the card bar is disk usage only and the card shows just "scanning…" — scan progress lives solely in the strip at the top (user). |
| 2026-09-15T11:15:00 | Switches, radio buttons and checkboxes styled in the accent (Mint-Y's blue bitmap glyphs replaced: radial-gradient radio dot, symbolic tick); disabled states dimmed. |
| 2026-09-15T12:10:00 | Overview list view (user's pick, default): ui/widgets/mount_list.py — disks as section rows, one row per mount with device, type, used, free, total, inline Used % bar, last scan and a star column; double-click or context menu scans, context menu also sets primary/secondary and opens the file manager; header sorting within each disk. "List | Cards" switcher remembered in settings (overview.view). No Map view. |
| 2026-09-15T12:50:00 | Content area background fixed to the #2d323d token (the theme's viewport was painting #343946 over it). Overview list restyled to the mockup: bordered cells, uppercase muted disk section rows on the surface colour, bold mount names with an outlined PRIMARY/SECONDARY badge, mono device/type, bar-only Used %, time-only scan stamp for today, star + Scan/Rescan button cell. |
| 2026-09-15T13:40:00 | Resize fix: the page stack now sizes to its visible page (hhomogeneous/vhomogeneous off) and the window minimum is 1000 px, so no page forces a layout wider than the window (which painted black). Explorer tree minimum 380 px. Overview list: outer border in the cell border colour, 4 px extra side padding in every cell. |
| 2026-09-15T15:10:00 | Cards view removed from the Overview (list only); Overview columns now reorderable, resizable and hideable from a header menu with Reset, persisted (overview.columns/hidden_columns/widths). Black-on-resize: every content GdkWindow gets the surface colour as its X background. Hardware cards: Copy is an icon. Sidebar: Snapshots → History (page restructure in WP17). Scan scheduler: lindrivespace --collect headless collector, systemd user timer managed from Settings › Background collection (interval, full scan, Run now, status); automatic history recording of usage per mount at each refresh and of every finished scan. |
| 2026-09-15T16:20:00 | WP17: History page — core/history.py HistoryStore (usage + scan samples, de-duped, JSON in ~/.cache/lindrivespace/history.json), TrendChart drawing area, Day/Week/Month/Year/All period chips, First/Latest/Change/Growth-per-day tiles, Pattern History bookmarks. Old Snapshots UI removed (tests/ui/test_snapshots.py deleted). |
| 2026-09-15T16:40:00 | Fix: black page gutters on scrolled pages (History/Hardware/Settings) on resize. Root cause: widget margins are painted by the parent and the ScrolledWindow/Viewport chain does not paint that band. Pages now carry the 24 px gutter as CSS padding (class page-padded) and paint it themselves; GdkWindow background hack removed. 0 black samples on every page at 1000/1400 px. |
| 2026-09-15T16:40:00 | History › Change tile is clickable (user): reveals "Where space changed" — a sortable folder/file table (Change · Before · After · Difference, icons, 300-row cap, Copy, double-click opens the path in the Explorer) diffed between the newest kept scan and the oldest one inside the selected period (core/changes.py). Every finished scan is now auto-saved as a compact snapshot (12 kept per root, worker thread). Fixed show_all() on no-show-all cards (also affected the Pattern History save row). |
| 2026-09-15T16:40:00 | Governance: adversarial-reviewer agent (Opus, read-only) added to .claude/agents — red-teams designs, methods and diffs, ranks risks with evidence and collaborates with implementer/code-reviewer agents via SendMessage; wired into routing-rules.md and /team. |
| 2026-09-15T17:05:00 | Fix: Hardware page "collecting" spinner never appeared (show_all() on a no-show-all box is a no-op); same fix as the History cards. |
| 2026-09-15T17:40:00 | Hardware (user): Disks and I/O activity moved to the top of the page; both tables restyled like the Overview list (bordered frame, bold muted headers, grid lines, 6 px cell padding, mono device/number columns, sortable, resizable; wider columns so nothing clips). |
| 2026-09-15T17:40:00 | Column selector menus (Explorer, Overview) (user): check items are grey when off and accent orange with a white tick when on — Mint-Y's blue bitmap glyph on "menu menuitem check" is overridden at that specificity. Applies to every menu check/radio in the app. |
| 2026-09-15T17:55:00 | docs/ISSUES-AND-RESOLUTIONS.md written (every issue met so far: symptom, root cause, fix, guard, symptom checklist); HANDOFF refreshed (state table, decisions, process, backups, open items); pending.md updated; local backup taken. |
| 2026-09-15T18:40:00 | History › Where space changed (user): Change and Difference cells colour-coded — growth/new in the danger red (bold), reductions/deleted in the ok green. |
| 2026-09-15T18:40:00 | Logging (user): folder and level configurable in Settings › Diagnostics (folder chooser + Default, level Debug/Info/Warnings, current log path, Open log folder); settings logging.dir/logging.level; logsetup.reconfigure() swaps the rotating file handler at runtime; app applies it at startup (--debug overrides). |
| 2026-09-15T18:40:00 | Overview (user): "Scanned" column removed; a persisted column order containing retired ids is cleaned instead of discarded. |
| 2026-09-15T18:40:00 | Fix: starring a mount in the Overview list raised ValueError on every click (PyGObject 3.48 returns 2 values from cell_get_position). Fix: the error bar can no longer raise from its own idle callback (the 2 648-traceback storm at 06:53) and a failing reporter is disabled. |
| 2026-09-15T18:40:00 | docs/BACKLOG.md: investigation of the click delay (Mint-Y 200 ms button transition, Glossary 190 ms show, per-click settings save, systemctl status), log errors (tree-selection assertion, log volume) and performance (retained scans → 186 MB after /home, 5.1 M-entry /mnt/data in 118 s, snapshot serialisation, lsblk on the main thread). |
| 2026-09-15T19:30:00 | Performance walkthrough (user): scripted click-through of every feature during and after the startup scan with a stall watchdog; verified root causes recorded in docs/BACKLOG.md §E — History diff/listing holding the GIL (thread vs fork measured), cyclic-GC full collections of 343 ms with 2.1 M live tree objects (0 ms after gc.freeze), per-folder top-files ring = 66 % of tree memory and 2.5× scan time (1 MB minimum fixes both), Glossary first layout. logsetup.reconfigure now raises the logger level. |
| 2026-09-15T20:30:00 | Packaging (user): bin/lindrivespace launcher (checkout-relative, or installed root substituted); scripts/install.sh (user ~/.local or --system /usr/local, launcher + menu entry + icons + metainfo + polkit helper, cache refresh) and uninstall.sh; scripts/build-deb.sh builds dist/lindrivespace_0.1.0_all.deb with dpkg-deb (postinst refreshes desktop/icon caches). data/com.mensuramedia.lindrivespace.desktop (System › LinDriveSpace, inode/directory) and .metainfo.xml — both validate. New application icon: dark rounded tile, orange usage ring, drive glyph; SVG + PNG 16–512. data_path() honours LINDRIVESPACE_DATA_DIR. README: real screenshots (docs/screenshots/) replace the mockup renders; install section rewritten. |
| 2026-09-15T21:10:00 | Sidebar logo reverted to the original ring mark (user) as a separate icon com.mensuramedia.lindrivespace-mark.svg; the tiled icon stays for the desktop menu. README screenshots re-captured. |
| 2026-09-15T22:30:00 | Performance fixes (user): (1) snapshot listing reads only the gzip header, alloc stored in meta; (2) services/forkwork.py — snapshot autosave and the History diff run in a forked child instead of a thread (GIL stalls of 0.3–1.8 s gone); (3) retained scan trees gc.freeze()'d during/after scans, dropped trees have their cycles broken in ScanTreeModel.reset() (343 ms full collections → 0); (4) ScanOptions.top_min_bytes = 64 KB (frozen contract, additive) + Settings › Scanning "Smallest file kept for analysis": tree memory 86 → 36 MB on /home, 2.6 % of bytes leave the analysis estimate; (5) Glossary bodies built on first expand; Mint-Y 200 ms button transition disabled. |
| 2026-09-15T23:10:00 | Performance verified with the same scripted walkthrough: stalls > 150 ms 105 → 1, slowest click 272 → 53 ms, RSS 1 525 → 593 MB, display suite 15 → 8 s (docs/BACKLOG.md §E). |
| 2026-09-15T23:40:00 | README (user): each screenshot now sits under its feature description (Explorer, Analysis, History, Favorites, Hardware, Glossary, Settings; Overview is the hero image); the separate gallery is a one-paragraph pointer. Stale "cards" wording removed; status line updated to v0.1 installable. |
| 2026-09-15T23:55:00 | Title bar (user): page name shown at the same size and weight as the app name, separated by an em dash ("LinDriveSpace — Overview"); page name a shade lighter. |
| 2026-09-16T00:15:00 | Title bar (user): app name, dash and page name rendered as one label with Pango markup — single baseline and ordinary word spacing (three boxed labels had uneven gaps around the dash). MainWindow.page_title / set_page_title replace subtitle_label. |
| 2026-09-16T00:40:00 | Header shows only the app name (user: the page is evident from the sidebar); sidebar logo area gets more space between the mark and the name, name bold. README hero is now the Explorer screenshot; Overview screenshot sits under its section. Screenshots re-captured. |
| 2026-09-16T01:00:00 | Fix (D1): the shared UI test window now uses its own config/cache/log dirs and an explicit Settings path; the test suite had written a column layout into the real ~/.config/lindrivespace/settings.json (found while re-capturing screenshots). Real settings restored to default column layouts, 64 KB threshold, 1200×800. |
